import { digitFormat, linkTo } from '../../../../comp/util';

Component({
    properties: {
        group: Object,
        currentServerTime: Number,
        isJoinedGroup: Boolean,
    },

    data: {
    },

    methods: {
    }
});